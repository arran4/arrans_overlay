--- a/src/caelestia/subcommands/shell.py
+++ b/src/caelestia/subcommands/shell.py
@@ -14,6 +14,13 @@

     def __init__(self, args: Namespace) -> None:
         self.args = args
+
+    @staticmethod
+    def qs_args(*args: str) -> list[str]:
+        path = "/usr/share/quickshell/caelestia"
+        if not os.path.exists(f"{path}/shell.qml"):
+            fatal("gui-apps/caelestia-shell is not installed. Please emerge gui-apps/caelestia-shell.")
+        return ["qs", "-p", path, *args]

     def run(self) -> None:
         if self.args.show:
@@ -38,10 +45,10 @@
             self.start_shell()

     def shell(self, *args: str) -> str:
-        return subprocess.check_output(["qs", "-c", "caelestia", *args], text=True)
+        return subprocess.check_output(self.qs_args(*args), text=True)

     def start_shell(self) -> None:
-        args = ["qs", "-c", "caelestia", "-n"]
+        args = self.qs_args("-n")
         if self.args.log_rules:
             args.extend(["--log-rules", self.args.log_rules])
         if self.args.daemon:
@@ -57,7 +64,7 @@
                         print(line, end="")

     def list_instances(self) -> list[dict]:
-        proc = subprocess.run(["qs", "-c", "caelestia", "list", "-j"], check=False, capture_output=True, text=True)
+        proc = subprocess.run(self.qs_args("list", "-j"), check=False, capture_output=True, text=True)
         if proc.returncode != 0:
             fatal(f"failed to list shell instances: {(proc.stderr or proc.stdout).strip()}")

@@ -92,7 +99,7 @@
         # exit. A kill that fails is not fatal here: the force kill below is the
         # fallback for any instance that does not go away.
         for pid in pids:
-            subprocess.run(["qs", "-c", "caelestia", "kill", "--pid", str(pid)], check=False, stdout=subprocess.DEVNULL)
+            subprocess.run(self.qs_args("kill", "--pid", str(pid)), check=False, stdout=subprocess.DEVNULL)

         # Teardown is not instant, so wait for the instances to actually disappear
         if self.wait_for_exit(5):
--- a/src/caelestia/subcommands/screenshot.py
+++ b/src/caelestia/subcommands/screenshot.py
@@ -1,8 +1,10 @@
+import os
 import subprocess
 from argparse import Namespace
 from datetime import datetime

 from caelestia.utils import hypr
+from caelestia.utils.io import fatal
 from caelestia.utils.notify import notify
 from caelestia.utils.paths import screenshots_cache_dir, screenshots_dir

@@ -21,8 +23,11 @@

     def region(self) -> None:
         if self.args.region == "slurp":
+            path = "/usr/share/quickshell/caelestia"
+            if not os.path.exists(f"{path}/shell.qml"):
+                fatal("gui-apps/caelestia-shell is not installed. Please emerge gui-apps/caelestia-shell.")
             subprocess.run(
-                ["qs", "-c", "caelestia", "ipc", "call", "picker", "openFreeze" if self.args.freeze else "open"]
+                ["qs", "-p", path, "ipc", "call", "picker", "openFreeze" if self.args.freeze else "open"]
             )
         else:
             sc_data = subprocess.check_output(["grim", "-l", "0", "-g", self.args.region.strip(), "-"])
